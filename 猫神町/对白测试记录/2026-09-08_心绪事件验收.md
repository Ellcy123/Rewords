# 心绪出牌事件首版验收 · 2026-09-08

执行者：明确指定 `gpt-5.6-terra` 的测试子代理。范围：小春、三种心绪牌的可执行后果基础设施；未操作玩家 5173/8787 的实际存档。

## 自动回归

- 9 个测试文件、169 项通过，类型检查与生产构建通过。
- 新增覆盖：出牌缺失后果时内存和存档完全回滚；NPC决定句才执行材料交付；待播后果不可提前结束丢失；存档恢复与扣牌关联；整理邀请、撤回、30分钟执行、重复执行和旧版本请求拒绝；全部材料已查看时不新建空整理任务；重复同时间地点会面不算新后果；暂停接待60分钟及期满恢复。
- 旧档兼容、自然拾绪、逐句播放、案件与赴约原有测试继续通过。

## 隔离离线浏览器

使用 `CAT_HEART_PREVIEW=1` 的内存后端8790和前端5174，不使用真实模型。

1. 在猫神社与小春开始拾绪，开场自然获得恐惧牌，加上测试补给共4张。
2. 出示恐惧后减为3张，遥表达时显示后果待播放，结束会面按钮不可用。
3. NPC决定播放后，事件面板和“共同整理遗物 · 30分钟”入口出现。
4. 刷新后邀请和12:00的时间保留。
5. 点击整理，时间变为12:30并返回场景；E01车票内容进入手记，背包无车票，实物所有者仍为小春。
6. 确认事件链 `heart_consequence → heart_activity → evidence_read → encounter_completed`。

测试后关闭隔离标签页及自己启动的5174/8790进程，没有关闭玩家原服务。

## 真实模型有界检查

前两次独立内存检查被 `player_intent` 拒绝并回滚。只提取了部分响应字段，未保留完整原始数据，因此无法可靠判断拒绝来自哪一环；不得据此断言生产代码存在特定回调错误。提取结构的离线回放能正常执行，但不替代原始响应诊断。

最终通过 `prototype-web/scripts/recordHeartConsequences.ts` 进行一次完整捕获：离线开场、真实 fear 出牌、生成最多尝试1次、原有内容审校最多1次；不加载或写入玩家数据库，不记录请求头或密钥。结果：生成与审校HTTP 200，审校 `approved=true`，失败为空。

本次实际生成（仅为测试样本，不写入正式剧情或玩家记忆）：

- 遥：我……其实也有点怕。怕自己整理不好姐姐留下的东西，怕辜负了托付给我的人。
- 小春：你也怕？我还以为只有我会这样。
- 遥：嗯。但怕归怕，该做的事还是得做。
- 小春：……那，你能陪我一起整理吗？我怕一个人。

四个节拍旁白均为空。`consequence.type=sorting_offer`，`beatIndex=3`，原文锚点为“那，你能陪我一起整理吗？我怕一个人。”。实际逐句播放后 `consequenceApplied=true`，`heart_spent` 的 event_8 与 `heart_consequence` 正确关联，来源NPC台词为 event_11。

结论：小春出牌到真实事件的首版闭环已验证；真实成功样本不代表所有牌型、所有情境和长对话的生成稳定性或事件多样性已验收。运行时没有新增第二个事件判断AI。
