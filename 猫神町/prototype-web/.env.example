# 将本示例复制到 server/.env.local 后填写；该私密文件不提交 Git。
PORT=8787

# 直接使用 DeepSeek 时填写官方地址；使用兼容中转时填写其 API 根路径。
# 以下为 DeepSeek 官方 Chat Completions 配置，不使用中转平台的模型前缀。
DEEPSEEK_API_KEY=
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-pro
